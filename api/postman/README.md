# Postman

Collection e ambientes do Postman serão adicionados após a API do laboratório
estar implementada.

Use variáveis como `{{base_url}}`, `{{token}}` e `{{user_id}}`.
Não exporte ambientes contendo valores secretos.

**Estado:** PLANEJADO.
